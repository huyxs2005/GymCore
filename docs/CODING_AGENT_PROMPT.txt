You are working on GymCore in D:\project.

Read these files first, in this order:
1. README.md
2. docs/LOCAL_SETUP_AND_VERIFY.txt
3. docs/Usecase functions.txt

Project summary:
- Backend: Spring Boot 4, Java 21, Maven wrapper in backend/mvnw.cmd
- Frontend: React 19, Vite, Tailwind, React Router, React Query
- Database: Microsoft SQL Server
- E2E: Playwright with the active config at playwright.config.js
- Shell: PowerShell

Important current local defaults:
- SQL Server: tcp:localhost,1433
- Database: GymCore
- Username: sa
- Password: 5

Seed data order:
1. docs/GymCore.txt
2. docs/alter.txt
3. docs/InsertValues.txt
4. docs/InsertTestingValues.txt

Helpful scripts:
- Seed DB: .\seed-db.ps1
- Start app: .\start-dev.ps1
- Full verify: .\verify-local.ps1

Real verification commands:
- Frontend:
  cd D:\project\frontend
  npm run lint
  npm run build
  npm run test:run
- Backend:
  cd D:\project\backend
  .\mvnw.cmd test
- Playwright:
  cd D:\project
  npx playwright test --config playwright.config.js --workers=1

Seeded login accounts:
- admin@gymcore.local / Admin123456!
- reception@gymcore.local / Reception123456!
- coach@gymcore.local / Coach123456!
- customer@gymcore.local / Customer123456!

Business rules that matter:
- Product review is allowed only after pickup is confirmed.
- PT booking AI assistant must stay aligned with the real PT booking match logic.
- Google login is optional and should only be treated as enabled when VITE_GOOGLE_CLIENT_ID is present.

Project hygiene:
- Use PowerShell commands.
- Prefer precise edits and avoid reverting user changes.
- After meaningful changes, rerun the relevant verification, and for broad changes rerun full local verification.
- The legacy .planning folder, old e2e template specs, and playwright.config.ts were removed intentionally. Do not recreate them unless the team explicitly asks.
